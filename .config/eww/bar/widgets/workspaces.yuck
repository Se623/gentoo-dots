;; Workspaces widget ;;

(deflisten wslist :initial "dcdsdc " "bash ~/.config/eww/bar/scripts/workspaces.bash")

(defwidget workspaces []
  (box :class "module module-active"
       (literal :content wslist)))
