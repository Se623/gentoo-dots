;; dashboard button ;;

(defwidget notif-btn []
        (box :class "module module-active module-notes-btn"
             :orientation "h"
                     (button :style "border-radius: 10px 0px 0px 10px;"
                             :onclick "dashboard-exec"
                             (label :class "icon-small"
				    :text "\\ue92b"
                                    :tooltip "Open dashboard panel"))))
